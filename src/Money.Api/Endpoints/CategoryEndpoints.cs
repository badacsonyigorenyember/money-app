namespace Money.Api.Endpoints;

/// <summary>
/// Placeholder so the composition root (Task 26) compiles and routes ahead of Task 27, which
/// replaces this with the real category routes (spec section 9).
/// </summary>
public static class CategoryEndpoints
{
    public static RouteGroupBuilder MapCategoryEndpoints(this RouteGroupBuilder group) => group;
}
