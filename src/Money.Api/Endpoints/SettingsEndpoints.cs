namespace Money.Api.Endpoints;

/// <summary>
/// Placeholder so the composition root (Task 26) compiles and routes ahead of Task 27, which
/// replaces this with the real settings routes (spec section 9).
/// </summary>
public static class SettingsEndpoints
{
    public static RouteGroupBuilder MapSettingsEndpoints(this RouteGroupBuilder group) => group;
}
